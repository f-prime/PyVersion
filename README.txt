PyVersion is a simple way to transfer files remotely.

Usage:

server.py: python server.py <port>

client.py: python client.py <list|del|send|get> <file name (if command is list just put "none" for filename)> <ip> <port>


